org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
kotlin_version=2.2.20
# Disabled: incremental cache relativizes paths between plugin sources (pub-cache, often on C:)
# and this project (D:), which crashes the Kotlin daemon with
# "this and base files have different roots" on Windows when they're on different drives.
kotlin.incremental=false
