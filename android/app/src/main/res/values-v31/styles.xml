<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ has its own SplashScreen API that otherwise ignores
         launch_background.xml and shows the launcher icon on a white
         circle mask. Point it at the same background/icon we use below. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_image</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/launch_background</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
